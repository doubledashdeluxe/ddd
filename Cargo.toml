[workspace]
members = [
    "formats",
    "server",
    "tools/obj2bin",
    "tools/obj2dol",
    "tools/split",
]
resolver = "3"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

[workspace.lints.clippy]
identity_op = "allow"

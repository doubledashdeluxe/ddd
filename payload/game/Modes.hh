#pragma once

#include "game/RaceMode.hh"

const u32 ModeCount = 5;
const u32 Modes[ModeCount] = {
        RaceMode::VS,
        RaceMode::Balloon,
        RaceMode::Escape,
        RaceMode::Bomb,
        RaceMode::TA,
};
